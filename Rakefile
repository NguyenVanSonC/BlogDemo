# Add your own tasks in files pl
# aced in lib/tasks ending in .rake,
# for example lib/tasks/capistr
# ano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
